# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase / env（.env 含敏感金鑰，唔好 commit）
.env
.env.*
!.env.example

# 本機工具暫存
npm-install.log
.npm-install.pid

# 獨立 git repo（約 780MB）—— 唔加入呢個 repo
ai-homework-follow-up/

# Firestore 保安規則：唔 commit（公開 repo 唔想暴露「邊啲 collection 開放」）
# 本機檔案照樣保留，部署時手動貼上 Firebase Console 即可。
firestore.rules

# Vercel 本機連結（.vercel/project.json 含 projectId / orgId，唔需要 commit）
.vercel

# Vercel CLI 自動產生嘅本機環境檔（含 VERCEL_OIDC_TOKEN，已經由上面 .env.* 覆蓋）
.env*
